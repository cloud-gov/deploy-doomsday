credentials:
- name: /tooling/doomsday
  type: json
  value:
    # proto_bosh:
    #   client_id: doomsday-readonly
    #   client_secret: ((????))
    #   hosts:
    #   - https://((/proto_bosh.ip)):8443
    #   - https://((/proto_bosh.ip)):8844
    #   - https://((/proto_bosh.ip)):25250
    #   - https://((/proto_bosh.ip)):25555
    tooling_bosh:
      client_id: doomsday-readonly
      client_secret: ((doomsday-readonly-secret))
      hosts:
      - https://((/tooling_bosh.ip)):8443
      - https://((/tooling_bosh.ip)):8844
      - https://((/tooling_bosh.ip)):25250
      - https://((/tooling_bosh.ip)):25555
      target: https://((/tooling_bosh.ip)):8844
    slack_webhook_url: ((/slack_webhook_url))




 